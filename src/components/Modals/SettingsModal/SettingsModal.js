import React from "react";

// move `Settings` stuff relative to the `SettingsModal` here, then instantiate directly in the `Settings` component.

/*
  TODO: work on form in `SettingsModal`, such that card styles and theme changes aren't submitted until after the user clicks `Submit`. Also, will need a `Cancel` button.
*/
